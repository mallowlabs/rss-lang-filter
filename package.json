{
  "name": "rss-lang-filter",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "generate:ngrams": "node scripts/generate-ngrams.mjs",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "hono": "^4.13.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@cloudflare/workers-types": "^5.20260812.1",
    "@types/xmldom": "^0.1.34",
    "efficient-language-detector-no-dynamic-import": "^1.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
